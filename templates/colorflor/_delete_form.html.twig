<form method="post" action="{{ path('colorflor_delete', {'idcolorflor': colorflor.idcolorflor}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ colorflor.idcolorflor) }}">
    <button class="btn">Borrar</button>
</form>
