<form method="post" action="{{ path('cliente_delete', {'codcliente': cliente.codcliente}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cliente.codcliente) }}">
    <button class="btn">Delete</button>
</form>
