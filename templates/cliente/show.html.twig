{% extends 'base.html.twig' %}

{% block title %}Cliente{% endblock %}

{% block body %}
    <h1>Cliente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Codcliente</th>
                <td>{{ cliente.codcliente }}</td>
            </tr>
            <tr>
                <th>Dni</th>
                <td>{{ cliente.dni }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cliente.nombre }}</td>
            </tr>
            <tr>
                <th>Apellidos</th>
                <td>{{ cliente.apellidos }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ cliente.telefono }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cliente_index') }}">Volver atrás</a>

    <a href="{{ path('cliente_edit', {'codcliente': cliente.codcliente}) }}">Editar</a>

    {{ include('cliente/_delete_form.html.twig') }}
{% endblock %}
