{% extends 'base.html.twig' %}

{% block title %}Parteutil{% endblock %}

{% block body %}
    <h1>Parteutil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id Parte-Útil</th>
                <td>{{ parteutil.idparteutil }}</td>
            </tr>
            <tr>
                <th>Parte</th>
                <td>{{ parteutil.parte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('parteutil_index') }}">Volver atrás</a>

    <a href="{{ path('parteutil_edit', {'idparteutil': parteutil.idparteutil}) }}">Editar</a>

    {{ include('parteutil/_delete_form.html.twig') }}
{% endblock %}
