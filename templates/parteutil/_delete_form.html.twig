<form method="post" action="{{ path('parteutil_delete', {'idparteutil': parteutil.idparteutil}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parteutil.idparteutil) }}">
    <button class="btn">Borrar</button>
</form>
