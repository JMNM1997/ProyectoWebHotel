{% extends 'base.html.twig' %}

{% block title %}Noticia{% endblock %}

{% block body %}
    <h1>Noticia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ noticium.codnoticia }}</td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td>{{ noticium.descripcion }}</td>
            </tr>
            <tr>
                <th>Imagen</th>
                <td>{{ noticium.imagen }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('noticia_index') }}">Volver atrás</a>

    <a href="{{ path('noticia_edit', {'codnoticia': noticium.codnoticia}) }}">Editar</a>

    {{ include('noticia/_delete_form.html.twig') }}
{% endblock %}
